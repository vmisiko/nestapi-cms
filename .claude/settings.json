{
  "permissions": {
    "allow": [
      "Bash(npm run lint *)",
      "Bash(npx eslint *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -qE '\\.ts$' && npx eslint \"$f\" --fix 2>&1 | head -30; } 2>/dev/null || true",
            "statusMessage": "Auto-fixing lint..."
          }
        ]
      }
    ]
  }
}
